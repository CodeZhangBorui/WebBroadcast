# WebBroadcast

## Setup

```bash
npm install -g yarn
yarn
```

## Project Structor

- `src` Source script
  - `web` Web dictionary
        - `page` Single page (Control by router)
        - `public` Static file
    - `lib` Helping scripts